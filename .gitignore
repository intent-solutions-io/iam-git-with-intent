# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
*.tsbuildinfo

# Internal package build artifacts (hooks are built at runtime)
internal/*/*.js
internal/*/*.js.map
internal/*/*.d.ts
internal/*/*.d.ts.map

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# Coverage
coverage/

# OS
.DS_Store
Thumbs.db

# Turbo
.turbo/

# GWI runtime state (local only)
.gwi/
.engine/

# Internal Dev Tooling (NEVER commit - local workspace only)
# These are internal tools for dev/ops evidence - NOT production
.agentfs/
.beads/

# Test artifacts
__snapshots__/
*.snap

# Workspace (internal dev/ops - ephemeral artifacts, planning, evidence)
# This entire directory is local-only, never committed
workspace/runs/

# OpenTofu / Terraform
infra/.terraform/
infra/*.tfstate
infra/*.tfstate.*
infra/.terraform.lock.hcl
infra/*.tfplan
*.backup

# bv (beads viewer) local config and caches
.bv/
